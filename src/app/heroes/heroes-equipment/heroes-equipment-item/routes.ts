import { Routes } from '@angular/router';
import { HeroesEquipmentItemComponent } from './heroes-equipment-item.component';

export const routes: Routes = [
    { path: '', component: HeroesEquipmentItemComponent },
];